# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.7.3
pydantic-settings==2.3.3

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# File handling
python-multipart==0.0.9
filetype==1.2.0

# Email
# (uses stdlib smtplib — no extra package)

# Background workers
celery[redis]==5.4.0
redis==5.0.6

# Virus scanning (optional — graceful fallback if clamd is not running)
pyclamd==0.4.0

# Rate limiting
slowapi==0.1.9

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0

# Phase 4 additions
boto3==1.34.131          # S3 / MinIO client
prometheus-client==0.20.0  # Prometheus metrics
sentry-sdk[fastapi]==2.5.1  # Error tracking (optional — set SENTRY_DSN to enable)
